# PFLM Profilometer Analysis — Python dependencies
# Tested on Python 3.13 (Windows). Versions listed are the tested minimums.
# tkinter itself ships with CPython and is not pip-installable.

# --- core analysis + plotting ---
numpy>=2.5.1
scipy>=1.18.0
pandas>=3.0.3
matplotlib>=3.11.0
ezdxf>=1.4.4          # DXF fabrication-geometry reader

# --- pflm_ui.py (optional desktop GUI) ---
Pillow>=12.3.0        # figure preview (PNG/JPG); PNG also works via Tk without it
tkinterdnd2>=0.6.2    # drag-and-drop for the DXF/VK4 panels (Browse buttons work without it)
