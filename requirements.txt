# PFLM Profilometer Analysis — Python dependencies
# Tested on Python 3.13 (Windows). Versions listed are the tested minimums.

# --- core analysis + plotting ---
numpy>=2.5.1
scipy>=1.18.0
pandas>=3.0.3
matplotlib>=3.11.0
ezdxf>=1.4.4          # DXF fabrication-geometry reader
statsmodels>=0.14.6  # optional: process-conditional diameter model (diameter_model.png/.txt); the
#                      figure is skipped with a note if this is absent, the rest of the run is fine

# --- desktop interface (pflm_app.py) ---
# The desktop UI. On Windows, enable long paths before installing or the wheel
# half-extracts against the 260-character limit and the QML plugins go missing.
PySide6>=6.11.1
